import './SoftHeader.css'

/**
 * Rick and morty data base
 * Soft header
 */
const SoftHeader = () => {
  return <section data-testid="soft-header" className='soft-header'>
    <h1>
      R&MDB
    </h1>
    Rick and morty data base, like imdb, but wayy cooler
  </section>
}

export default SoftHeader;